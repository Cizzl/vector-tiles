{
  "name": "vector-tile-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --hot run server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
